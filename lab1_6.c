#include<stdio.h>

int main()
{
    int n,m,t;
    char a;
    scanf("%d %d %d",&n,&m,&t);
    fflush(stdin)
    scanf("%c",&a)
     for(i = 0; i < N; i++)
    {
        scanf("%d", &n);

        if(i==0 || n > max)
        {
            max = n;
            lo = i+1;
        }
    }
     printf("%d %d",lo,max);

    return 0;
}
