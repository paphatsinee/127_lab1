#include<stdio.h>
#include<math.h>
int main()
{
    int n,b,sum;
    scanf("%d",&n);
    if(n>=0&&n<=1000000000)
    {
        if(b>=0&&b<=1000000000)
        {
            sum=a+b;
            printf("%d",sum);
        }
    }
    return 0;
}
